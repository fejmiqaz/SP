#include<iostream>
using namespace std;

/* Write a function that removes all occurrences of a specified substring on a given string using pointers.
 * The removeSubstring(char mainStr, const char substr[]) function takes two parameters - the main string (mainStr) and the
 * substring to be removed (substr).*/

int main(){

    return 0;
}